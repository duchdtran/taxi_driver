import 'package:flutter/material.dart';

class AppSizedBox extends SizedBox {
  const AppSizedBox.height(double height) : super(height: height);
  const AppSizedBox.width(double width) : super(height: width);
}